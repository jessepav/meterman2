boolean objectAction(Entity e, Action action, Entity selectedEntity) {
    if (action.equals(SystemActions.CONTAINER_PUT)) {
        gm.println("Why would you want to put a stick in " + selectedEntity.getDefName() + "?");
        return true;
    } else {
        return false;
    }
}
