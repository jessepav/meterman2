package com.illcode.meterman2.text;

/**
 * A {@code TextSource} that comprises a list of other sources, and whose text output is
 * the normalized concatenation (separated by spaces) of each {@code TextSource} in sequence.
 */
public class CompoundSource implements TextSource
{
    public String getText() {
        return null;
    }
}
